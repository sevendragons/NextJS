# NextJS - Practice Guide

## `npm run build`
## `npm run export`
## `now login`
## `npm run devploy`

###  "start": "NODE_ENV=production node server.js" in package JSON to run offline

#### Change folder's name `out' to the name you want to create the host. 👍️.